name: Apply frontend code splitting

on:
  push:
    branches:
      - auditoria-codex-parcial
    paths:
      - scripts/apply-frontend-code-splitting.mjs
      - .github/workflows/apply-frontend-code-splitting.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout branch
        uses: actions/checkout@v7
        with:
          ref: auditoria-codex-parcial
      - name: Set up Node.js
        uses: actions/setup-node@v7
        with:
          node-version: 22
      - name: Apply patch
        run: node scripts/apply-frontend-code-splitting.mjs
      - name: Commit patch
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/App.tsx src/Dashboard.tsx
          if git diff --cached --quiet; then
            echo "No changes to commit"
            exit 0
          fi
          git commit -m "perf: lazy load dashboard modules"
          git push origin HEAD:auditoria-codex-parcial
