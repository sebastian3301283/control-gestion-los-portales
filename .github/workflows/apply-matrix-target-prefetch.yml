name: Apply matrix target prefetch

on:
  push:
    branches:
      - auditoria-codex-parcial
    paths:
      - scripts/apply-matrix-target-prefetch.mjs
      - .github/workflows/apply-matrix-target-prefetch.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout branch
        uses: actions/checkout@v7
        with:
          ref: auditoria-codex-parcial
      - name: Set up Node.js
        uses: actions/setup-node@v7
        with:
          node-version: 22
      - name: Apply patch
        run: node scripts/apply-matrix-target-prefetch.mjs
      - name: Commit patch
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/PlanningGuidelines.tsx src/GuidelineCatalogV2.tsx src/UnitExcelWorkspace.tsx src/CentralExcelWorkspace.tsx
          if git diff --cached --quiet; then
            echo "No changes to commit"
            exit 0
          fi
          git commit -m "perf: prefetch exact matrix rows before navigation"
          git push origin HEAD:auditoria-codex-parcial
